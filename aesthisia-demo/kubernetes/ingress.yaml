apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aesthisia-ingress
  namespace: aesthisia
  annotations:
    kubernetes.io/rewrite-target: nginx
spec:
  IngressClassName: nginx
  rules:
    - host: aesthisia.mydevopslearn.xyz
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: aesthisia-svc
                port:
                  number: 80