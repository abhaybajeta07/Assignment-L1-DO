apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aesthisia-ingress
  namespace: aesthisia-react
  annotations:
    kubernetes.io/rewrite-target: nginx
spec:
  rules:
    - host: react.aesthisia.in
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: aesthisia-react-svc
                port:
                  number: 80